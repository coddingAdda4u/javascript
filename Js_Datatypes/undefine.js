// Undefined: A variable that has been declared but not assigned a value.

let val2;
console.log(val2);
console.log(typeof(val2));

// const val; // Error, because const variable cant't be undefined