// Ternary Operator**: A shorthand for  if else 
// condition ? valueIfTrue : valueIfFalse 

// Assigning variables
let age = 18;
let canVote = age >= 18 ? "Yes, you can vote." : "No, you cannot vote.";
console.log(canVote); // true, because age is greater than 18